import 'package:flutter/widgets.dart';

const spaceXS = 4.0;
const spaceS = 8.0;
const spaceM = 16.0;
const spaceL = 32.0;
const spaceXL = 64.0;

const paddingXS = EdgeInsets.all(4.0);
const paddingS = EdgeInsets.all(8.0);
const paddingM = EdgeInsets.all(16.0);
const paddingL = EdgeInsets.all(32.0);
const paddingXL = EdgeInsets.all(64.0);

const borderRadiusXS = BorderRadius.all(Radius.circular(4.0));
const borderRadiusS = BorderRadius.all(Radius.circular(8.0));
const borderRadiusM = BorderRadius.all(Radius.circular(16.0));
const borderRadiusL = BorderRadius.all(Radius.circular(32.0));
const borderRadiusXL = BorderRadius.all(Radius.circular(64.0));

const durationXS = Duration(milliseconds: 100);
const durationS = Duration(milliseconds: 200);
const durationM = Duration(milliseconds: 400);
const durationL = Duration(milliseconds: 600);
const durationXL = Duration(milliseconds: 800);
